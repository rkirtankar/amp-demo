{
  "name": "amp-server-functions",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "node --inspect node_modules/.bin/functions-framework --source=build/ --target=handler"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.2.0",
    "escape-html": "^1.0.3"
  }
}
